[package]
name = "decoder-wasm"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
qoi = "0.4"

[profile.release]
opt-level = "s"
lto = true
strip = true
